<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed for Google TTS audio and the Reader's PDF.js/JSZip CDNs. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- navigator.vibrate() (correct-answer haptic) is a no-op without this. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Speak tab: getUserMedia()/SpeechRecognition from the WebView need both
         this manifest permission AND a runtime grant (handled in
         MainActivity's onPermissionRequest) — WebView doesn't forward mic
         access the way a full browser app does. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <!-- Settings → Check for updates: lets the app launch the system
         installer for an APK it downloaded itself, instead of the user
         going through a browser download + file manager each time. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Android 11+ (API 30+) package-visibility: without declaring this,
         the app can't see whether anything on the device handles speech
         recognition at all, even when it does — WebView's SpeechRecognition
         then fails immediately (surfaced to the page as a generic
         "not-allowed" error, indistinguishable from an actual mic-permission
         denial) because its own availability check comes back empty. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
