# OS / editor cruft
.DS_Store
Thumbs.db
*~
.vscode/
.idea/

# Local server logs (not part of the app)
*.log

# Android build artifacts
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/captures/
*.iml
# Web app is copied here by CI at build time — not source
android/app/src/main/assets/www/
# Built APK
tiengviet.apk

# Local backup of the signing keystore — NEVER commit (key + password)
keystore-backup/
